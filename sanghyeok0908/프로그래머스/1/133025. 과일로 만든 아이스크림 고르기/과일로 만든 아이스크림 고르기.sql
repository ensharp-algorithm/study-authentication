-- 코드를 입력하세요
SELECT FIRST.FLAVOR FROM FIRST_HALF FIRST INNER JOIN ICECREAM_INFO INFO ON FIRST.FLAVOR = INFO.FLAVOR WHERE FIRST.TOTAL_ORDER >= 3000 AND INFO.INGREDIENT_TYPE = 'fruit_based' ORDER BY FIRST.TOTAL_ORDER DESC